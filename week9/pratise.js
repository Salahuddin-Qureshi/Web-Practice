let promise = Promise(function(myResolve, myReject){
    let x = 0;
    if (x == 0){
        myResolve("OK");
    }
    else{
        myReject("Reject");
    }

});
//promise.then
promise.then(
    function(value) {myDis}


);
